# PERG波形解析プロジェクト：依存ライブラリ
#
# 【重要・再現性について】
# 検出ロジック（detect_perg_waves.py の detect_perg_waves 関数）は、
# 境界値ギリギリのケース（プロミネンス閾値0.3uV、low_confidence閾値1.0uV付近）で、
# ライブラリのバージョン差によりごく僅かに異なる結果を返すことを実際に確認している
# （2026-09-17、同一データ・同一コードで2つの実行結果を比較し、1354件中130件で
#  数値の相違、うち5件でlow_confidenceフラグの反転を確認）。
#
# そのため、以下のバージョンを固定でインストールすることを強く推奨する：
#   pip install -r requirements.txt
#
# detect_perg_waves.py は実行時にこのファイルの内容と実際の環境を照合し、
# 一致しない場合は警告を表示する。

numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
matplotlib==3.10.9
